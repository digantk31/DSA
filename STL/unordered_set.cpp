#include <bits/stdc++.h>
using namespace std;

int main()
{
    // only for understanding purpose (There will be not a output of this Programm)

    unordered_set<int> st;
    //    lower_bound and upper_bound function does not works, rest all functions are same as multiset.
    //  it does not stores in any particular order.
    //  it has a better complexity [O(1)] than set in most cases, except some when collision happens. but in worst case [one in a million complexity is [O(n)].

    return 0;
}